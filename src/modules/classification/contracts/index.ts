/**
 * HTTP contracts of categories and tags.
 *
 * Route handlers and a later browser client share these types and mappers so
 * the documented representation cannot drift between the two sides.
 */

export * from "./category";
export * from "./tag";
